<table>
  <% var i;
    for(i=0; i<quizes.length; i++){ %>
    <tr>
      <td>
        <span><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %></a></span>
      </td>
    </tr>
  <% } %>
</table>
